services:
  bank-postgres:
    image: postgres
    container_name: bank-postgres
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bank
    ports:
      - "5432:5432"
    volumes:
      - ${DB_MOUNT_PATH}/postgres.sql:/docker-entrypoint-initdb.d/postgres.sql